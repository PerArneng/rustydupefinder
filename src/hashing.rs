
use digest::Digest;
use sha2::Sha256;
use std::io::{Read};
/*
fn hash(digest: &Digest, reader: &Read) -> Box<[u8]> {

    return String::from("").into_boxed_bytes();
}

pub fn create_hash() -> String {
    println!("hi");

    let sha256 = Sha256::new();

    //hash(&sha256, )


    return String::from("hi");
}


#[test]
fn create_hash_test() {
    create_hash();
}*/